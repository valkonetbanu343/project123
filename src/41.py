def add_numbers(x: int, y: int) -> int:
    """
    Add two numbers.
    
    Args:
        x (int): First number.
        y (int): Second number.
        
    Returns:
        int: The sum of x and y.
    """
    return x + y

if __name__ == "__main__":
    print(add_numbers(5, 10))
